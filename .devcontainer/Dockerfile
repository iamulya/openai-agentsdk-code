FROM mcr.microsoft.com/devcontainers/python:3.12-bullseye

# Install Node.js and npm for the filesystem server example
RUN curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && \
    apt-get install -y nodejs